pipeline {
    agent any
    stages {
        stage("Set Working Directory") {
            agent {
                docker {
                    reuseNode true
                    image 'node:10.20.1-alpine3.11'
                }
            }
            steps {
                sh '''
                node -v
                ls
                npm -v
                cd ui_testing
                ls
                '''
            }
        }
        stage("Install dependencies") {
            steps {
                sh 'npm ci'
            }
        }
        stage("UI Testing") {
            steps {
                sh 'npm test'
            }
        }
    }
}
