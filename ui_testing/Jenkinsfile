pipeline {
    agent any
    stages {
        stage("Set Working Directory") {
            agent {
                docker {
                    image 'node:16.15-alpine'
                    reuseNode true
                }
            }
            steps {
                sh '''
                node -v
                npm -v
                cd ui_testing
                npm ci
                '''
            }
        }
        stage("Install dependencies") {
            steps {
                sh 'npm ci'
            }
        }
        stage("UI Testing") {
            steps {
                sh 'npm test'
            }
        }
    }
}
