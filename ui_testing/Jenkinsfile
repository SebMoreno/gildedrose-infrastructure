pipeline {
  agent {
    docker { image 'cypress/base:10' }
  }
  stages {
    stage('build and test') {
      steps {
        dir('ui_testing') {
          sh 'npm ci'
          sh 'npm test'
        }
      }
    }
  }
}
