pipeline {
    agent any
    tools { nodejs 'node' }
    environment {
        CYPRESS_BASE_URL = 'http://3.232.104.87'
    }
    stages {
        stage('Install dependencies') {
            steps {
                dir('ui_testing') {
                    sh 'npm ci'
                }
            }
        }
        stage('UI Testing') {
            steps {
                dir('ui_testing') {
                    sh 'npm test'
                }
            }
        }
    }
}
