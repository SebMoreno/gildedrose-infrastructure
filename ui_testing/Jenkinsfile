pipeline {
    agent any
    stages {
        stage("Set Working Directory") {
            agent {
                docker {
                    image 'node:16.15.0'
                    reuseNode true
                }
            }
            steps {
                sh '''
                node -v
                npm -v
                cd ui_testing
                npm i
                '''
            }
        }
        stage("Install dependencies") {
            steps {
                sh 'npm ci'
            }
        }
        stage("UI Testing") {
            steps {
                sh 'npm test'
            }
        }
    }
}
