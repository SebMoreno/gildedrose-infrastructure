pipeline {
    agent any
    stages {
        stage("Set Working Directory") {
            agent {
                docker {
                    reuseNode true
                    image '14.19.3-alpine3.15'
                }
            }
            steps {
                sh '''
                node -v
                ls
                npm -v
                cd ui_testing
                ls
                npm ci
                '''
            }
        }
        stage("Install dependencies") {
            steps {
                sh 'npm ci'
            }
        }
        stage("UI Testing") {
            steps {
                sh 'npm test'
            }
        }
    }
}
